const ReaderContainer = ({ children }) => (
  <div className='flex items-center justify-center min-h-screen rounded-lg'>
    <div className='relative flex items-center w-full max-w-4xl'>{children}</div>
  </div>
);
export default ReaderContainer;
